<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    
    <item
        android:id="@+id/action_label"
        android:icon="@drawable/ic_done"
        android:title="@string/done"
        app:showAsAction="always" />

    <item
        android:id="@+id/rotation_image"
        android:icon="@drawable/ic_rotate"
        android:title="@string/rotate"
        app:showAsAction="always" />

    <!-- New "Use Original" button – appears before cropping -->
    <item
        android:id="@+id/use_original"
        android:title="Use Original"
        app:showAsAction="never" />

    <group
        android:id="@+id/enhance_group"
        android:checkableBehavior="single">
        <item
            android:id="@+id/gray"
            android:icon="@drawable/ic_done"
            android:title="@string/black" />
        <item
            android:id="@+id/reset"
            android:icon="@drawable/ic_reset_colors"
            android:title="@string/reset" />
    </group>
</menu>